version: 2

sources:
  - name: postgres__employees
    description: 'Source schema from MySQL containing employee data'
    tables:
      - name: employee_count
        description: 'Employee count table from source system'

models:
  - name: employee_count
    description: 'Employee count model migrated to Redshift analytics schema'
    config:
      materialized: table
      schema: analytics
