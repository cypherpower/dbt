name: 'informatica_migration'
version: '1.0.0'
config-version: 2

profile: 'migrate_loop_duaye4qnvi3yxtvnkuzxy_1'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  informatica_migration:
    +materialized: table
    +schema: analytics
    staging:
      +materialized: view
      +schema: analytics
    marts:
      +materialized: table
      +schema: analytics